Output a single integer $k$ --- the optimal value of $k>1$ for which a $k$-ary search on a table of size $n$ achieves the most efficient search cost.If there are multiple answers, you should output the smallest.